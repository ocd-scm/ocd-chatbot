#!/bin/sh

cd /tmp && wget -q -O - https://github.com/github/hub/releases/download/v2.4.0/hub-linux-amd64-2.4.0.tgz | \
    tar zxf - && \
    mv $(find . -name hub) /usr/local/bin

exec $STI_SCRIPTS_PATH/run
